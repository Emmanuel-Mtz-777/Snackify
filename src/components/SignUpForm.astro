---
import { Image } from 'astro:assets';
import logo from '../../public/Img/Icons/logo.webp';
---

<section class="signUp w-full max-w-md h-auto bg-[#0000008c] rounded-2xl p-6 mx-auto my-6">
  <a href="/">
    <div class="w-full h-[70px] flex items-center justify-center gap-4">
      <Image src={logo} alt="Logo de Snackify" class="h-[70px] w-[70px]" />
      <h3 class="text-[#ddd] font-bold text-3xl">Snackify</h3>
    </div>
  </a>

  <form class="flex flex-col justify-center mt-6 space-y-5">
    <h1 class="font-bold text-4xl text-white text-center">Crear Cuenta</h1>

    <div class="flex flex-col text-white">
      <label for="username" class="text-xl mb-1">Nombre</label>
      <input type="text" required id="username" class="p-2 w-full border-b outline-none bg-transparent placeholder:text-gray-300" />
    </div>

    <div class="flex flex-col text-white">
      <label for="email" class="text-xl mb-1">Dirección de email</label>
      <input type="email" required id="email" placeholder="example@gmail.com" class="p-2 w-full border-b outline-none bg-transparent placeholder-[#89CFF0]" />
    </div>

    <div class="flex flex-col text-white">
      <label for="password" class="text-xl mb-1">Contraseña</label>
      <input type="password" required id="password" class="p-2 w-full border-b outline-none bg-transparent" />
    </div>

    <div class="flex flex-col text-white">
      <label for="confirm-password" class="text-xl mb-1">Confirmar Contraseña</label>
      <input type="password" required id="confirm-password" class="p-2 w-full border-b outline-none bg-transparent" />
    </div>

    <div class="text-[#eee] text-sm">
      <p>
        Al identificarte aceptas nuestras 
        <a href="/" class="text-sky-800 underline"> Condiciones de uso y venta</a>. Consulta nuestro 
        <a href="#" class="text-sky-800 underline"> Aviso de privacidad</a>.
      </p>
    </div>

    <div class="flex justify-center">
      <button class="w-[70%] p-2 rounded-2xl text-lg font-bold text-amber-50 border border-amber-50 hover:bg-amber-50 hover:text-black transition-all">
        Crear Cuenta
      </button>
    </div>

    <div class="flex items-center gap-4 mt-4 text-gray-400">
      <div class="flex-grow border-t border-gray-400/30"></div>
      <span>¿Ya tienes cuenta?</span>
      <div class="flex-grow border-t border-gray-400/30"></div>
    </div>

    <div class="flex justify-center">
      <a href="/login" class="w-[90%] p-2 text-xl font-bold rounded-2xl text-center text-amber-50 border border-amber-50 hover:bg-amber-50 hover:text-black transition-all">
        Iniciar Sesión
      </a>
    </div>
  </form>
</section>

<style>
  .signUp {
    backdrop-filter: blur(5px);
  }

  @media screen and (max-width: 560px) {
    .signUp {
      width: 90%;
      margin: 2rem auto;
    }
  }
</style>
